#include <iostream>

/*int main() {
	int num;
	std::cin >> num;

	for (int i = 0; i < num; i++) {
		int a, b, n;
		std::cin >> a >> b >> n;
		int count = 0;

		while (a <= n && b <= n) {
			if (a < b)
				a += b;
			else
				b += a;
			count++;
		}

		std::cout << count << std::endl;
	}

	return 0;
}*/